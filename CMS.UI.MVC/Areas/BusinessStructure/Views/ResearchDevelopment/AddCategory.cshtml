@model CMS.MODEL.Entities.Category
@{
    ViewBag.Title = "AddCategory";
    Layout = "~/Areas/BusinessStructure/Views/Shared/_Layout.cshtml";
}

    <div class="col-md-12">
        <h2>Ar-Ge</h2>

        <div class="m-portlet m-portlet--tab">

            <div class="m-portlet__head">
                <div class="m-portlet__head-caption">
                    <div class="m-portlet__head-title">
                        <span class="m-portlet__head-icon m--hide">
                            <i class="la la-gear"></i>
                        </span>
                        <h3 class="m-portlet__head-text">
                            Kategori Ekle
                        </h3>
                    </div>
                </div>
            </div>
            @using (Html.BeginForm("AddCategory", "ResearchDevelopment", FormMethod.Post))
            {
                <form class="m-form m-form--fit m-form--label-align-right">
                    <div class="m-portlet__body">
                        <div class="form-group m-form__group">
                            <label>Kategori Adı</label>
                            <input type="text" class="form-control m-input" name="CategoryName" placeholder="Kategori Adı">
                        </div>
                        <div class="form-group m-form__group">
                            <label>Açıklama</label>
                            <input type="text" class="form-control m-input" name="Description" placeholder="Açıklama">
                        </div>
                        <div class="form-group m-form__group">
                            <div class="m-form__actions">
                                <button type="submit" class="btn btn-success">Ekle</button>
                            </div>
                        </div>
                    </div>
                    
                </form>
            }
            </div>


    </div>

